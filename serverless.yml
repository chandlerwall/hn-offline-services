service:
  name: hn-offline-services

plugins:
  - serverless-offline
  - serverless-webpack

provider:
  name: aws
  region: us-west-2
  runtime: nodejs8.10

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello

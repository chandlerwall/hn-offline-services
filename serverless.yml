service:
  name: hn-offline-services

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack

provider:
  name: aws
  region: us-west-2
  runtime: nodejs8.10

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
